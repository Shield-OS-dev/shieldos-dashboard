import { AppShell } from "./app/AppShell";

// Placeholder entry.
// When the real frontend stack is decided (Next.js, Vite, etc.),
// this file can be adapted to the chosen framework.

export function bootstrapDashboard() {
  // eslint-disable-next-line no-console
  console.log("ShieldOS Dashboard bootstrap placeholder");
  return AppShell;
}
